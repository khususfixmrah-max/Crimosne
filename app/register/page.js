 "use client";import{useState}from"react";import Link from"next/link";import{createClient}from"../../lib/supabase-browser";export default function Register(){const[n,setN]=useState(""),[e,setE]=useState(""),[p,setP]=useState(""),[err,setErr]=useState(""),[msg,setMsg]=useState("");async function submit(x){x.preventDefault();setErr("");setMsg("");const{error}=await createClient().auth.signUp({email:e,password:p,options:{data:{full_name:n}}});if(error)setErr(error.message);else setMsg("Akun dibuat. Cek email jika verifikasi aktif.")}return <main className="container"><div className="form card"><Link className="brand" href="/">Crimosne OTP</Link><h1>Daftar</h1>{err&&<div className="alert">{err}</div>}{msg&&<div className="success">{msg}</div>}<form onSubmit={submit}><label className="label">Nama</label><input className="input" required value={n} onChange={x=>setN(x.target.value)}/><label className="label">Email</label><input className="input" type="email" required value={e} onChange={x=>setE(x.target.value)}/><label className="label">Password</label><input className="input" type="password" minLength="8" required value={p} onChange={x=>setP(x.target.value)}/><button className="btn">Buat Akun</button></form><p className="muted">Sudah punya akun? <Link href="/login">Masuk</Link></p></div></main>}
